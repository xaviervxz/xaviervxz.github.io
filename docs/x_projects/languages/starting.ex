hd [3.14, :pie, "Apple"]
map = %{:foo => "bar", "hello" => :world}
map[:foo]

defmodule Example.Greetings do
  def morning(name) do
    "Good morning #{name}."
  end

  def evening(name) do
    "Good night #{name}."
  end
end
